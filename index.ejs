<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fetch and Save MP3</title>
</head>
<body>
    <h1>Fetch and Save MP3</h1>
    <div id="output">
        <% if (message) { %>
            <p><%= message %></p>
        <% } else { %>
            <p>No message available</p>
        <% } %>
    </div>
</body>
</html>